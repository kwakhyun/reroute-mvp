# REROUTE MVP 검증 보고서

검증일: 2026-09-01

대상: Next.js standalone 프로덕션 빌드, 사용자 데이터와 분리한 로컬 SQLite QA 데이터베이스, GitHub Actions의 초기화된 실행 환경

## 자동 검증

| 항목 | 결과 |
| --- | --- |
| ESLint, 경고 허용 0건 | 통과 |
| TypeScript `tsc --noEmit` | 통과 |
| Vitest | 14개 파일, 45개 테스트 통과 |
| 핵심 정책 모듈 커버리지 | 문장 87.58%, 분기 80.37%, 함수 96.49%, 라인 90.28% |
| Next.js 16 standalone 프로덕션 빌드 | 통과 |
| 전체 의존성 `npm audit --audit-level=moderate` | 알려진 취약점 0건 |

커버리지 수치는 전체 UI나 모든 라우트를 대상으로 하지 않습니다. `vitest.config.ts`에 지정한 CSV 파서, 날짜 처리, DB URL과 마이그레이션, 프록시, 접근 정책, 로그인 제한, 매칭 정책과 엔진의 범위를 뜻합니다.

기존 데이터가 있는 0001 스키마에 다섯 번째 자산군과 배정 데이터를 넣은 뒤 0002, 0003을 순서대로 적용하는 업그레이드 테스트를 포함했습니다. 기존 데이터에 연결할 수 없는 항목이 있으면 삭제하지 않고 마이그레이션을 중단합니다.

실제 SQLite 트랜잭션을 동시에 실행한 통합 테스트에서는 재계산과 확정 중 하나만 반영되고 확정 초안은 보존되는 것을 확인했습니다.

## 브라우저 전체 흐름

실제 standalone 서버와 격리된 QA 데이터베이스를 연결해 앱 내 브라우저에서 다음 흐름을 검증했습니다.

1. 승인자 계정으로 로그인해 샘플 프로젝트의 자산별 최소 회수액 합계, 추천 지표, 자산과 수요처 배정, 수요처 검증 근거를 확인했습니다.
2. CSV로 새 프로젝트를 만들고, 프로젝트 자산 ID가 포함된 템플릿으로 검증 근거가 포함된 입찰 네 건을 가져왔습니다.
3. 새 프로젝트의 매칭안을 계산하고 성공 상태를 확인한 뒤, 자산별 배정을 검토하고 확정했습니다.
4. 확정 결과가 수거 1회와 외부 결제사 미연동 정산 상태로 전달되고, 감사 로그에 프로젝트 생성, 입찰 가져오기, 재계산과 확정 기록이 남는지 확인했습니다.
5. 다른 조직 프로젝트의 페이지와 API에 직접 접근해도 데이터를 노출하지 않고 404를 반환하는지 확인했습니다.
6. 운영 CSP의 `script-src`에 `unsafe-inline`과 `unsafe-eval`이 없고 모든 Next.js 스크립트가 요청별 nonce를 갖는지 원본 응답으로 확인했습니다. 필요한 이미지와 경로 변경 알림의 스타일 속성만 `style-src-attr`에서 허용합니다.
7. 390 × 844 화면에서 본문이 가로로 넘치지 않는지 확인했습니다. 열린 모바일 메뉴는 배경을 `inert`로 처리하고 포커스를 메뉴 안에 가둔 뒤, 닫을 때 메뉴 버튼으로 돌려보냅니다.
8. 준비된 DB의 `/api/health`가 `ready`와 200을, 빈 DB가 `not_ready`와 503을 반환하는지 확인했습니다. 두 응답 모두 API 완료 로그에 상태 코드, 지연 시간과 요청 ID를 남겼습니다.
9. 수요처 검증 근거가 서울 날짜 기준 만료일까지 유효하고 다음 날부터 추천과 확정에서 제외되는지 정책 테스트로 확인했습니다.

CI용 Playwright 전체 여정은 [tests/e2e/matching-journey.spec.ts](./tests/e2e/matching-journey.spec.ts)에 포함했습니다. 재시도할 때마다 고유한 프로젝트를 만들고 준비 상태, 익명 접근, CSP, 404, 샘플 추천안, 입찰 가져오기, 재계산, 확정, 수거, 정산과 감사 로그를 검증합니다. 같은 검증 묶음은 [GitHub Actions 실행](https://github.com/kwakhyun/reroute-mvp/actions/runs/33494714440)의 초기화된 환경에서도 통과했습니다.

## 판정과 잔여 경계

[2차 심층 감사](./artifacts/audit-2026-09-01-round2/audit-report.md)에서 발견한 P0 1건, P1 8건, P2 11건은 모두 구현 또는 검증 근거를 보완해 해결했습니다. 자세한 내역은 [개선 완료 보고서](./artifacts/audit-2026-09-01-round3/resolution-report.md)에 정리했습니다.

현재 로컬 검증 범위에서 열린 P0, P1, P2는 없습니다. GitHub Actions 검증은 완료했지만, 실제 Turso의 다중 리전 경쟁 동작과 Vercel 배포 실행은 아직 확인하지 않았습니다.

외부 결제사와 운송사 연동, 실사용자 스크린리더 평가, 대규모 부하와 침투 테스트도 운영 전환 전에 별도로 검증해야 합니다.

**최종 결과: 문서에 명시한 로컬 및 CI 검증 범위에서 통과**
