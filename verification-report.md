# REROUTE MVP 검증 보고서

테스트 57개, E2E 8개와 라인 커버리지 90.76%는 이전 버전의 검증 기록입니다. 이후 달라진 측정 범위, 화면과 동시성 검증의 경계는 [변경 이력](./docs/version-history.md)에 설명했습니다.

## 2026-09-05 개선 후 로컬 검증

대상: 현재 작업 트리의 Next.js standalone 빌드, `/private/tmp` 아래 별도 libSQL 파일 DB, Chromium. 기존 3000번 개발 서버와 원격 DB는 사용하지 않았습니다. 이번 변경은 아직 원격 배포나 CI에서 실행하지 않았습니다.

| 항목 | 결과 |
| --- | --- |
| ESLint, TypeScript | 경고 및 오류 없이 통과 |
| Vitest | 19개 파일, 93개 테스트 통과 |
| 커버리지 | 문장 86.80%, 분기 79.02%, 함수 95.32%, 라인 89.27% |
| Next.js standalone 빌드 | 통과 |
| Playwright Chromium | 12개 시나리오 통과, 1440px / 1280px / 1024px / 768px / 640px / 390px 화면 포함 |
| 데모 리셋 | 세션과 타 조직 데이터 보존 회귀 검증 통과 |
| 분석 재현성 | 산출물 6개 일치, 노트북 오류 0건 |
| 변경 형식 검사 | `git diff --check` 통과 |

커버리지 측정 범위에 실제 입찰 서버 액션, 매칭 변경 서비스, 미리보기 서명과 로컬 DB 트랜잭션을 추가했습니다. 전체 UI나 모든 라우트의 수치가 아니며, 이전에 기록한 범위의 커버리지와 직접 비교하지 않습니다. 기존 기준인 문장·라인·함수 80%, 분기 75%를 낮추지 않고 통과했습니다.

### 수정한 문제와 확인 방법

1. **검토한 배분안만 확정**: 확인창의 배분안 ID와 프로젝트 버전을 고정하고 서버에서 재검증합니다. A가 검토한 뒤 B가 재계산한 경우, 그리고 서버의 읽기와 트랜잭션 사이에 변경이 끼어든 경우 모두 확정을 거절하고 수거, 정산, 확정 감사 기록을 만들지 않습니다.
2. **갱신 중 재진입 차단**: 최신 배분안을 받는 동안 진행 상태와 `aria-disabled`를 표시하고 실행 핸들러도 차단합니다. 키보드 포커스를 유지하면서 이전 결과로 확인창을 다시 여는 틈을 막습니다. 브라우저 응답을 900ms 지연시켜 검증했습니다.
3. **CSV 빈 숫자 거절**: 매각 금액, 비용 절감액, 수량과 비율 등 숫자 열의 공란은 행과 열을 알려줍니다. 명시적으로 입력한 허용 범위의 `0`은 계속 받습니다.
4. **확인 자료 일관성**: 같은 자료의 인수처 이름, 유형, 확인 내용과 만료일이 다르면 거절합니다. 유효기간이 공란인 행이 먼저 오거나 나중에 와도 기존 자료를 변경하지 않는지 검증했습니다.
5. **교체 미리보기**: 기존/새 입찰 수, 삭제될 초안, 조합 수, 계산 가능 여부와 처음 10행을 표시합니다. 미리보기에서는 DB가 바뀌지 않으며, 교체 단계는 사용자와 파일, 프로젝트 버전, 15분 만료 시각에 묶인 서명을 확인합니다. 서명 변조, 다른 파일/사용자/버전, 시간 만료, 권한 변경을 검증했습니다.
6. **계산 한도 사전 확인**: 자산 17개에 후보가 각각 2개인 131,072개 조합과 계산 시간 초과는 교체 전에 차단합니다. 미리보기 후 자정이 지나 확인 자료가 만료된 경우도 교체하지 않습니다.
7. **실제 서비스 경쟁 테스트**: SQL 모형을 복제하던 테스트를 실제 서비스 호출로 교체했습니다. 네이티브 파일 DB의 겹치는 트랜잭션에서 재현된 오류는 파일별 실행 대기열로 보완했고, 두 연결의 동시 요청 중 하나만 반영되는지와 실패 후 다음 요청이 가능한지 확인했습니다. 원격 DB에는 이 대기열을 적용하지 않습니다.
8. **제품 소개의 정보 순서**: 로딩, 오류 복구, 서버 상태 동기화와 접근성 사례를 가상 사업 지표보다 앞에 배치했습니다. 시뮬레이션은 펼쳐 보는 보조 자료로 옮겼고, 새 카드의 글자색은 밝은 배경에 맞게 분리했습니다.

근거: [서비스 통합 테스트](./src/server/db/concurrency.test.ts), [입찰 액션 통합 테스트](./src/app/actions/bids.test.ts), [브라우저 시나리오](./tests/e2e/matching-journey.spec.ts).

### 추가 디자인, 구조와 성능 개선

기능 개선을 반영한 standalone 빌드에서 Chromium 12개 시나리오를 모두 통과했습니다. 이후 표 설명의 간격을 수정한 최신 빌드에서는 해당 반응형 시나리오를 다시 검증했습니다. 기존 흐름에 신규 프로젝트의 준비 전 안내를 추가했고, 별도 시나리오로 121행 목록의 필터/페이지 이동/전체 내보내기와 중간 화면 폭을 검증했습니다. 내보내기도 실제 로그인한 브라우저 세션으로 확인했습니다.

- 인수처와 입찰을 50행 단위로 저장하고 인수처 갱신은 UPSERT로 통합했습니다. 5,000행 실제 액션 테스트에서 기존/신규 인수처 혼합 교체와 DB 트리거로 유도한 중간 실패의 전체 롤백을 확인했습니다. 최초 커버리지 실행에서는 이 대용량 테스트만 30초 실행 제한을 넘었습니다. 해당 테스트 제한을 90초로 조정한 뒤 93개가 모두 통과했으며 앱의 계산 제한은 바꾸지 않았습니다.
- CSV 액션에서 순수 업무 검증과 가져오기 서비스를 분리했습니다. 커버리지에는 분리한 파일과 새 입찰 조회 서비스를 포함했습니다.
- 자산, 배분안 요약과 수거 상세의 조회 범위를 분리했습니다. 입찰은 50건씩 조회하며 동점 정렬, 페이지 보정, 필터 조합, 전체 내보내기와 조직 권한 경계를 실제 DB 테스트로 확인했습니다.
- 정상 프로젝트의 배분안 부재를 수거/정산 준비 전 안내로 처리했습니다.
- 지표 이름 줄바꿈, 중간 화면 폭의 표 배치, 실제 넘침을 감지하는 스크롤 안내와 키보드 탐색을 적용했습니다. 표 아래 설명을 일반 문서 흐름에 배치하고 다음 카드와 겹치지 않는지도 검증합니다. CSV 가져오기는 펼쳐 시작합니다.
- 전역 CSS 4,018행을 공통 스타일 354행과 6개 CSS Modules로 분리했습니다. 모듈로 이동한 애니메이션의 keyframes도 함께 한정했습니다. 공개 소개의 CSS에 업무 화면 스타일이 포함되지 않는 것을 빌드 결과에서 확인했습니다.
- 소개 화면은 1280px에서도 데모와 소스 링크가 첫 화면에 들어오도록 조정했습니다. 대표 이미지는 반응형 변환과 Next.js 16의 `preload`를 사용합니다.
- 이전에 시간 초과했던 65,536조합을 3회 모두 완료했습니다. 로컬 계산 시간은 324.14ms, 229.14ms, 127.31ms입니다. 40개 입력 묶음의 독립 전수 평가로 선택 결과와 동률 처리를 비교했습니다. 측정 조건과 한계는 [성능 검증 문서](./docs/performance.md)에 기록했습니다.

### 실행 조건

- Node.js 22.21.0, Next.js 16.3.3, 로컬 standalone 서버 포트 3017
- 단위 및 통합 테스트: `npm run test:coverage -- --maxWorkers=1`
- E2E: `PLAYWRIGHT_PORT=3017 PLAYWRIGHT_USE_PRODUCTION=true npm run test:e2e -- --workers=1 --timeout=60000`
- E2E 서버에는 별도 임시 `DATABASE_URL`, 빈 `DATABASE_AUTH_TOKEN`, 임시 `SESSION_PEPPER`, `DEMO_MODE=true`, `ALLOW_FILE_DATABASE=true`, `TRUST_PROXY_HEADERS=false`를 전달하고 로그 드레인은 비활성화했습니다.
- 로컬 실행 환경의 IPC 제한 때문에 마이그레이션, 시드와 데모 리셋 검증은 `node --import tsx scripts/<script>.ts`로 실행했습니다.

의존성 감사, 원격 Turso 및 Vercel 배포, 다중 프로세스·리전 장애 복구, Firefox/WebKit와 실제 스크린리더 사용자 평가는 이번 검증 범위에 포함하지 않았습니다. 실제 사용자 성능 지표도 수집하지 않았습니다.

---

아래는 이전 시점의 기록입니다. 당시 원격 배포와 CI 통과 결과가 이번 변경분까지 검증했다는 의미는 아닙니다.

## 이전 검증 기록 (2026-09-01)

검증일: 2026-09-01

대상: Next.js standalone 프로덕션 빌드, 로컬 SQLite QA 데이터베이스, Turso 원격 데이터베이스, Vercel 프로덕션 배포, GitHub Actions의 초기화된 실행 환경

## 자동 검증

| 항목 | 결과 |
| --- | --- |
| ESLint, 경고 허용 0건 | 통과 |
| TypeScript `tsc --noEmit` | 통과 |
| Vitest | 17개 파일, 57개 테스트 통과 |
| 핵심 정책 모듈 커버리지 | 문장 88.33%, 분기 81.49%, 함수 96.82%, 라인 90.76% |
| Next.js 16 standalone 프로덕션 빌드 | 통과 |
| Vercel 프로덕션 빌드와 Turso 상태 확인 | 통과 |
| 전체 의존성 `npm audit --audit-level=moderate` | 알려진 취약점 0건 |

커버리지 수치는 전체 UI나 모든 라우트를 대상으로 하지 않습니다. `vitest.config.ts`에 지정한 CSV 파서, 날짜 처리, 운영 입력 검증, DB URL과 마이그레이션, 프록시, 접근 정책, 로그인 제한, 매칭 정책과 엔진의 범위를 뜻합니다.

기존 데이터가 있는 0001 스키마에 다섯 번째 자산 항목과 배정 데이터를 넣은 뒤 0002, 0003을 순서대로 적용하는 업그레이드 테스트를 포함했습니다. 기존 데이터에 연결할 수 없는 항목이 있으면 삭제하지 않고 마이그레이션을 중단합니다.

실제 SQLite 트랜잭션을 동시에 실행한 통합 테스트에서는 재계산과 확정 중 하나만 반영되고 확정 초안은 보존되는 것을 확인했습니다.

## 브라우저 전체 흐름

실제 standalone 서버와 격리된 QA 데이터베이스를 연결해 앱 내 브라우저에서 다음 흐름을 검증했습니다.

1. 승인자 계정으로 로그인해 샘플 프로젝트의 자산별 최소 매각 금액 합계, 추천 지표, 자산과 인수처 배정, 인수처 확인 자료를 확인했습니다.
2. CSV로 새 프로젝트를 만들고, 프로젝트 자산 ID가 포함된 템플릿으로 인수처 확인 자료가 포함된 입찰 네 건을 가져왔습니다.
3. 새 프로젝트의 배분안을 계산하고 성공 상태를 확인한 뒤, 자산별 배정을 검토하고 확정했습니다.
4. 확정 결과가 수거 1회와 외부 결제사 미연동 정산 상태로 전달되고, 감사 로그에 프로젝트 생성, 입찰 가져오기, 재계산과 확정 기록이 남는지 확인했습니다.
5. 다른 조직 프로젝트의 페이지와 API에 직접 접근해도 데이터를 노출하지 않고 404를 반환하는지 확인했습니다.
6. 운영 CSP의 `script-src`에 `unsafe-inline`과 `unsafe-eval`이 없고 모든 Next.js 스크립트가 요청별 nonce를 갖는지 원본 응답으로 확인했습니다. 필요한 이미지와 경로 변경 알림의 스타일 속성만 `style-src-attr`에서 허용합니다.
7. 390 × 844 화면에서 본문이 가로로 넘치지 않는지 확인했습니다. 열린 모바일 메뉴는 배경을 `inert`로 처리하고 포커스를 메뉴 안에 가둔 뒤, 닫을 때 메뉴 버튼으로 돌려보냅니다.
8. 준비된 DB의 `/api/health`가 `ready`와 200을, 빈 DB가 `not_ready`와 503을 반환하는지 확인했습니다. 두 응답 모두 API 완료 로그에 상태 코드, 지연 시간과 요청 ID를 남겼습니다.
9. 인수처 확인 자료가 서울 날짜 기준 만료일까지 유효하고 다음 날부터 추천과 확정에서 제외되는지 정책 테스트로 확인했습니다.
10. 수거와 정산 상태를 연속 저장해 요약과 선택값이 계속 일치하는지 확인하고, 두 탭에서 같은 수거 건을 수정했을 때 늦은 저장이 최신 값을 덮어쓰지 않는지 검증했습니다.
11. 준비 완료 이후에는 수거지, 시간대, 차량과 담당자가 모두 필요하며, 모달을 닫은 뒤 실행 버튼으로 포커스가 돌아오는지 확인했습니다.

CI용 Playwright E2E 시나리오는 [tests/e2e/matching-journey.spec.ts](./tests/e2e/matching-journey.spec.ts)에 포함했습니다. 재시도할 때마다 고유한 프로젝트를 만들고 준비 상태, 익명 접근, CSP, 404, 샘플 추천안, 입찰 가져오기, 재계산, 확정, 수거, 정산과 감사 로그를 검증합니다. 같은 검증 묶음은 [GitHub Actions 실행](https://github.com/kwakhyun/reroute-mvp/actions/runs/33507330868)의 초기화된 환경에서도 통과했습니다.

## 판정과 잔여 경계

[2차 심층 감사](./artifacts/audit-2026-09-01-round2/audit-report.md)에서 발견한 P0 1건, P1 8건, P2 11건은 모두 구현을 수정하거나 확인 자료를 보완해 해결했습니다. 자세한 내역은 [개선 완료 보고서](./artifacts/audit-2026-09-01-round3/resolution-report.md)에 정리했습니다.

현재 검증 범위에서 열린 P0, P1, P2는 없습니다. [Vercel 프로덕션 배포](https://reroute-mvp-pied.vercel.app)는 Turso 원격 데이터베이스의 마이그레이션과 준비 상태까지 확인했습니다. 다중 리전 사이의 장거리 경쟁 동작과 장애 복구 훈련은 아직 확인하지 않았습니다.

외부 결제사와 운송사 연동, 실사용자 스크린리더 평가, 대규모 부하와 침투 테스트도 운영 전환 전에 별도로 검증해야 합니다.

**최종 결과: 문서에 명시한 로컬, 원격 데이터베이스, CI와 Vercel 배포 범위에서 통과**
