# REROUTE MVP 검증 보고서

검증일: 2026-09-01

대상: Next.js standalone 프로덕션 빌드, 사용자 데이터와 분리한 로컬 SQLite QA 데이터베이스

## 자동 검증

| 항목 | 결과 |
| --- | --- |
| ESLint, 경고 허용 0건 | 통과 |
| TypeScript `tsc --noEmit` | 통과 |
| Vitest | 14개 파일, 45개 테스트 통과 |
| 핵심 정책 모듈 커버리지 | 문장 87.58%, 분기 80.37%, 함수 96.49%, 라인 90.28% |
| Next.js 16 standalone 프로덕션 빌드 | 통과 |
| 전체 의존성 `npm audit --audit-level=moderate` | 알려진 취약점 0건 |

커버리지 수치는 전체 UI나 모든 라우트의 커버리지가 아니다. `vitest.config.ts`에 포함한 CSV 파서, 날짜, DB URL과 마이그레이션, 프록시, 접근 정책, 로그인 제한, 매칭 정책과 엔진의 범위를 뜻한다.

기존 데이터가 있는 0001 스키마에 다섯 번째 자산군과 배정을 넣고 0002, 0003을 순차 적용하는 업그레이드 테스트를 포함했다. 매핑할 수 없는 슬롯은 조용히 삭제하지 않고 마이그레이션을 중단한다. 실제 SQLite 트랜잭션을 경쟁시킨 통합 테스트에서는 재계산과 확정 중 하나만 커밋되고 확정 초안은 보존됨을 확인했다.

## 브라우저 전체 흐름

실제 standalone 서버와 격리된 QA 데이터베이스를 연결해 앱 내 브라우저에서 다음 흐름을 검증했다.

1. 승인자 계정으로 로그인하고 시드 프로젝트의 자산별 회수 기준액 합계, 추천 지표, 자산과 수요처 배정, 파트너 증빙을 확인했다.
2. CSV로 새 프로젝트를 생성하고, 프로젝트 자산 ID가 포함된 템플릿 형식으로 증빙 포함 입찰 네 건을 가져왔다.
3. 새 프로젝트의 매칭안을 계산하고 성공 상태를 확인한 뒤, 자산별 배정을 검토하고 확정했다.
4. 확정 결과가 수거 1회와 외부 결제사 미연동 정산 상태로 인계되고 감사 로그에 생성, 입찰 가져오기, 재계산, 확정이 남는지 확인했다.
5. 다른 조직 프로젝트의 페이지와 API 직접 접근이 데이터 노출 없이 404로 끝나는지 확인했다.
6. 운영 CSP의 `script-src`에 `unsafe-inline`과 `unsafe-eval`이 없고 모든 Next.js 스크립트가 요청별 nonce를 갖는지 원본 응답으로 확인했다. 필요한 이미지와 라우트 알림의 스타일 속성만 `style-src-attr`에서 허용한다.
7. 390 × 844에서 본문 가로 넘침이 없고, 열린 모바일 메뉴가 배경을 `inert` 처리하며 포커스를 가둔 뒤 닫을 때 메뉴 버튼으로 복귀하는지 확인했다.
8. 준비된 DB의 `/api/health`가 `ready`와 200을, 빈 DB가 `not_ready`와 503을 반환하는지 확인했다. 두 응답 모두 API 완료 로그에 상태 코드, 지연, 요청 ID를 남겼다.
9. 파트너 검증 근거가 서울 날짜 기준 만료일까지 유효하고 다음 날부터 추천과 확정에서 제외되는지 정책 테스트로 확인했다.

CI용 Playwright 전체 여정은 [tests/e2e/matching-journey.spec.ts](./tests/e2e/matching-journey.spec.ts)에 포함했다. 재시도마다 고유 프로젝트를 생성하며 준비 상태, 익명 접근, CSP, 404, 시드 추천안, 입찰 가져오기, 재계산, 확정, 수거, 정산, 감사 로그까지 검증한다. 이번 최종 로컬 판정은 앱 내 브라우저로 직접 실행한 결과를 기준으로 하며 GitHub Actions의 원격 실행 성공을 주장하지 않는다.

## 판정과 잔여 경계

[2차 심층 감사](./artifacts/audit-2026-09-01-round2/audit-report.md)의 P0 1건, P1 8건, P2 11건은 모두 구현 또는 검증 근거 수정으로 닫았다. 해결 내역은 [개선 완료 보고서](./artifacts/audit-2026-09-01-round3/resolution-report.md)에 연결했다.

현재 로컬 검증 범위에서 열린 P0, P1, P2는 없다. 다만 실제 Turso의 다중 리전 경쟁 동작, GitHub Actions와 Vercel 배포, 외부 결제사와 운송사, 실사용자 스크린리더, 대규모 부하와 침투 테스트는 아직 실행하지 않았다. 이 항목은 운영 전환 전 별도 검증이 필요하다.

**Final result: passed within the documented local verification scope**
